The core software for the girlvoice soft CPU

# Some helpful commands:
Build the firmware with
```
cargo build
```

Dump the firmware to assembly with:
```
cargo objdump --release -- --disassemble
```